import { Routes } from '@angular/router';
import { UnsubscriptionComponent } from './';

export const unsubscriptionRoutes: Routes = [
    {
        path: 'unsubscription',
        component : UnsubscriptionComponent
    }
]
